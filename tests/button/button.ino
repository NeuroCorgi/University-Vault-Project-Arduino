#include "common.h"

void setup() {
    setupPins();
}

void loop() {
    // int cState = digitalRead(A0);
    // if (cState == LOW) {
    //     delay(10);
    //     cState = digitalRead(A0);
    //     if (cState == LOW) {
    //         digitalWrite(A1, HIGH);
    //     }
    // }
    // digitalWrite(A1, LOW);
}